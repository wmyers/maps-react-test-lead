export type SearchParams = {
  price?: string;
  deposit?: string;
  term?: string;
  interest?: string;
};

export type FormInput = {
  price: string;
  deposit?: string;
  term: string;
  interest: string;
};
