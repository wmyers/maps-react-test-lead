export type FormInput = {
  price: string;
  deposit: string;
  term: string;
  interest: string;
};

export type FormValues = {
  price: number;
  deposit: number;
  term: number;
  interest: number;
};
